apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-wallet-pvc
  namespace: gobank
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-wallet
  namespace: gobank
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-wallet
  template:
    metadata:
      labels:
        app: postgres-wallet
    spec:
      containers:
        - name: postgres-wallet
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: gobank_wallet
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: gobank-app-secret
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: gobank-app-secret
                  key: POSTGRES_PASSWORD
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command: ["sh", "-c", "pg_isready -U gobank -d gobank_wallet"]
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            exec:
              command: ["sh", "-c", "pg_isready -U gobank -d gobank_wallet"]
            initialDelaySeconds: 20
            periodSeconds: 10
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: postgres-wallet-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-wallet
  namespace: gobank
spec:
  selector:
    app: postgres-wallet
  ports:
    - port: 5432
      targetPort: 5432
